# 定时任务清单（自进化闭环）

> 最后更新：2026-09-17（**升级为双周期自进化**）
> 项目：qmtIDE-deepseek ｜ 执行模式：`paper`
> 所有自动化统一使用 `C:\Users\lisinan\.conda\envs\qmt\python.exe`，运行前需 `export PATH=/c/Users/lisinan/.conda/envs/qmt/Library/bin:/c/Users/lisinan/.conda/envs/qmt/DLLs:$PATH`（否则 sqlite3 报 DLL 缺失），并 `export PYTHONPATH=.`。

## 〇、用户硬要求（2026-09-17 明确）

- **始终使用 `paper` 测试收益**；真实交易由用户**手工切换**，任何自动化不得写入 `EXECUTION_MODE="live"`。
- 所有数据用户会自己查看；自动化只负责产出与记账，不代替用户决策。
- 用户已授权按交易节奏进行**项目 + 参数自进化**，不再有"参数禁区"，但任何改动必须过晋升闸门。
- 因此 2026-09-17 起自进化频率**翻倍**：每个交易日拆为**上午 / 下午两个自进化周期**。

## 一、闭环总览（双周期版）

```
GUARD 存活兜底(每小时,含 paper 断言) ─┐
                                      ├─► PRE-MARKET 盘前排雷 09:05（核对两周期改动生效）
                                      │        │
                                      │        ▼
                                      │   【上午盘 09:30-11:30】
                                      │        │
                                      │        ▼
                                      │   ★ AM-EVOLVE 上午自进化 11:40（午休窗口）
                                      │      · 半日度量 + 候选扫描/回测 + 闸门落盘
                                      │      · 热读参数 → 13:00 下午开盘即生效（当天可检验）
                                      │        │
                                      │        ▼
                                      │   【下午盘 13:00-15:00】
                                      │        │
                                      │        ▼
                                      │   OBSERVE 盘后复盘 15:35 ──► EVOLUTION_LEDGER 账本
                                      │      · 含「上午改动当日实证：证实/证伪」
                                      │        │
                                      │   工程维护 17:00 ◄────────────┤
                                      │        │                      │
                                      │        ▼                      │
                                      │   ★ PM-EVOLVE 下午自进化 18:00 ┘
                                      │      · 全日度量 + 多折 consensus + 闸门落盘（次日生效）
                                      │      · 可承接需改引擎实现的重候选
                                      │        │
                                      └────────┴─► config/settings.py + EVOLUTION_DECISIONS.md
                                            周度深度 SYNTHESIZE 周六 10:00
                                            （双周期协同复盘 + churn 检查 + 结构性候选）
```

**双周期分工要点**

| | AM-EVOLVE 11:40（午休） | PM-EVOLVE 18:00（收盘后） |
|---|---|---|
| 数据新鲜度 | 半日 | 全日（OBSERVE 已入账） |
| 落盘生效时点 | 热读参数 **13:00 下午盘即生效** | 一律**次日开盘**生效 |
| 承接候选类型 | 优先热读参数，轻量、当天可验证 | 可承接需改 `engine/event_engine.py` 或需重启的重候选 |
| 必须做的接力 | 读上一轮「下一步聚焦方向」 | 读今日 AM 条目并给实证结论；收尾检查 AM/PM 是否冲突 |

**关键设计**：过去闭环只"记录发现"、从不落地能改收益的变更。**EVOLVE 执行器**是 2026-09-16 补齐的缺失环节——它按晋升闸门真的把改动落盘到 `config/settings.py` 并记账。2026-09-17 起拆为 AM/PM 双周期，进化机会由每日 1 次提升到**每日 2 次（每周最多 10 次）**。

## 二、任务明细

| # | 环节 | 自动化 ID | 名称 | 触发时间 | 核心职责 |
|---|---|---|---|---|---|
| 1 | GUARD | `ebde5d30` | 守护兜底（每小时） | 周一至周五 每小时 | 确认引擎(5000)与 miniQMT 在线；掉线触发自启动并复验；**顺带断言 `EXECUTION_MODE==paper`**（若为 live 只告警、不自动改回）。**只告警不拉起 miniQMT**（用户 2026-09-15 已禁用自动拉起，改为手动登录） |
| 2 | PRE-MARKET | `98ffc7a2` | 盘前就绪 09:05 | 周一至周五 09:05 | 在线检查、**确认 `EXECUTION_MODE=paper`**、查陈旧熔断、**核对最近两条（昨日下午 + 今日上午周期）进化改动是否生效**并注明生效方式（热读 / 需重启） → 产出 `reports/premarket_YYYY-MM-DD.md` |
| 3 | **AM-EVOLVE** | **`789d671e`** | **上午自进化 11:40** | 周一至周五 **11:40**（午休） | **每日第一个自进化周期**：半日度量 → 选 1~2 候选 → `_evolve_wf.py` 网格/对比/**多窗口 consensus** → 过闸门则落盘。优先热读参数，**下午 13:00 开盘即生效、当天可检验** |
| 4 | OBSERVE | `ae0e2431` | 盘后复盘 + 度量 15:35 | 周一至周五 15:35 | `strategy/review_daily.py` → 四维评分；`strategy/record_ledger.py` → **跨日真实收益**写入账本；**给出「上午周期改动的当日实证：证实/证伪/数据不足」**；追加 `OPTIMIZATION_BACKLOG.md`；交付 HTML 复盘报告 |
| 5 | 工程维护 | `2534a859` | 工程维护 + 策略改进落地 17:00 | 周一至周五 17:00 | db 体积体检/按需裁剪、日志轮转体检、`tests/run_all.py` 回归门禁、重复告警降噪。**收益类候选优先留给 18:00 PM-EVOLVE**，避免与两周期同日抢改同一参数 |
| 6 | **PM-EVOLVE** | **`d8013e93`** | **下午自进化 18:00** | 周一至周五 18:00 | **每日收官自进化周期**：全日度量 → 接力今日 AM 的「下一步方向」并给实证结论 → consensus 验证 → 过闸门则落盘（**次日生效**）。**可承接需改引擎实现的重候选**；收尾检查 AM/PM 是否冲突 |
| 7 | SYNTHESIZE | `f093d93b` | 周度深度 周六 10:00 | 周六 10:00 | 汇总本周真实表现、跑 `strategy/_verify_live_quality.py` 全量复验、**双周期协同复盘（落盘/否决统计 + churn 检查）**、提出**结构性**候选并验证 → 产出 `reports/EVOLUTION_PROPOSAL_YYYY-MM-DD.md` |

> 进化机会：改造前每日 1 次（18:00），改造后**每日 2 次（11:40 + 18:00）**，每周最多 10 次。

## 三、考核目标（未达之前持续迭代）

> **2026-09-19 OWNER 裁决修订**：原目标 `wf 均值 Sharpe ≥ 2.0` 在当前价格动量范式下**物理不可达**（天花板 +1.52，详见 `reports/EVOLUTION_PROPOSAL_2026-09-19.md` §四）。修订为 **≥ 1.6**（天花板留 +0.08 余量，仅新数据轴可突破）；并**新增「相对等权买入持有 alpha」真实价值门槛**——因全样本 alpha = −111.2pt 揭示策略真价值是风控而非收益生成。同日 OWNER **批准新数据轴（主力资金流）研发**以突破天花板。晋升闸门（单候选 +0.10 等）**不变**。

> **重心定位（2026-09-19 OWNER 二次裁决「转投北向轴 / 重定位重心」）**：Sharpe 1.6 仅为「不退化底线」，**主 KPI 是相对等权买入持有 alpha ≥ −30pt**——全样本 alpha=−111.2pt 揭示策略真价值在风控（MDD −15%、Sharpe 1.5+ 稳健）而非收益生成。新数据轴研发（北向/earnrev）一律以「能否把 alpha 从 −111pt 拉向 −30pt」为成败判据，绝不再以冲 Sharpe 2.0 为导向（该天花板 +1.52 物理不可达，已双重证伪）。

| 指标 | 目标 | 当前基线 |
|---|---|---|
| walk-forward 样本外均值 Sharpe | **≥ 1.6**（原 2.0） | **+1.52**（修正基线后全量复验，2026-09-19；天花板 +1.52） |
| walk-forward 正收益折数 | **≥ 6/7** | 6/7 |
| walk-forward 最大回撤 | **≥ −22%** | −15.36% |
| live paper 近 4 周滚动收益 | **> 0%** | 区间 −19.63%（样本不足，暂以区间近似） |
| 相对等权买入持有全样本 alpha | **≥ −30pt（新增）** | **−111.2pt**（策略真价值=风控，非收益生成） |

达标后可降级为低频观察；未达标则每个交易日继续迭代。

> **口径说明**：`strategy/_evolve_wf.py` 用 25 只 AI 宇宙 × 713 根日线（2022-12-08→2026-08-25），
> 与历史上「+339.9% / Sharpe 1.69」的 500 根短窗口口径不同，**绝对数值不可直接横比**。
> 自 2026-09-17 起，所有结论一律以**同口径 P0 基线的相对增量**为据（`_evolve_wf.py` 每次运行都会先打印 P0 基线）。
> 待办：live paper 4 周样本仍不足（账本自 2026-09-16 起累积），该目标暂以区间收益近似判定。

## 四、晋升闸门（EVOLVE / 工程维护落地参数前**全部**满足，缺一即拒绝）

1. 样本内（IS）收益或 Sharpe 相对当前配置有提升
2. **样本外（OOS）walk-forward 均值 Sharpe 较当前配置提升 ≥ +0.10**（OOS 是唯一决策依据）
3. OOS 与 IS 的 Sharpe 差距 ≤ 25%（差距过大 = 过拟合，拒绝）
4. walk-forward 最大回撤 ≥ −22%
5. 任一单折收益 > −15%（不得有灾难折）
6. `python tests/run_all.py` 全量通过
7. **风险底线不可动**：`max_drawdown_pct ≤ −0.15`、`risk_per_trade ≤ 0.02`、T+1 约束保持、`max_positions ≥ 3`
8. **★ 晋升闸门双通道（2026-09-23 OWNER 授权增设）**：
   - `--track alpha`（默认，用于**调参找 alpha**）：闸门② 要求最差窗口 dSh ≥ **+0.10**
   - `--track defect`（**缺陷修复**，用于消除「实盘行为 ≠ 配置语义」）：闸门② 放宽为
     「方向不劣化」——均值 dSh ≥ 0 且最差窗口 ≥ −0.05；**其余闸门①③④⑤⑥⑦⑧ 一条不放宽**。
     人工判据：DR-a 参数零改动 / DR-b 实盘铁证 / DR-c 守卫测试存在。
     缺陷修复必须用 `--baseline <实盘现状代理>` 作**反向对照**，否则 dSh 与闸门① 符号会判反。
   - 理由：+0.10 是为防 alpha churn 设计，套在缺陷修复上会**系统性阻断所有修复**。
8. `EXECUTION_MODE` 保持 `paper`（严禁切 live）

9. **防 churn**：同一参数 **24 小时内不得重复改动**（落盘前先查 `EVOLUTION_DECISIONS.md` 最近条目）；单轮最多落盘 2 个参数
10. **回测/实盘口径纪律**：落盘前必须确认该参数在**实盘引擎**有读取点。反例 `reentry_cooldown` 只存在于回测器 `BacktestConfig`、实盘未实现 → 禁止只写 `settings.py`，须先补 `engine/event_engine.py` 实现再走闸门

未通过则**不改动任何文件**，只在 `reports/EVOLUTION_DECISIONS.md` 记 REJECTED 与完整负面证据（务必保留，避免重复踩坑）。

### 回测工具（统一入口）

`strategy/_evolve_wf.py`（25 只 AI 宇宙 × 713 根日线 2022-12→2026-08，含单边 0.15% 成本）：

| 模式 | 命令 | 用途 |
|---|---|---|
| grid | `--mode grid --param <参数> --values <值列表>` | OOS 网格扫描，**找参数高原、拒绝尖峰** |
| compare | `--mode compare` | IS + 多折逐折明细 + 闸门裁决 |
| **consensus** | `--mode consensus` | **落盘的唯一决策依据**：同一候选在 4 套互不重叠折划分（60x9/75x7/90x6/120x4）重跑，取**最差窗口**防窗口运气 |

> **易错点**：闸门③（OOS/IS Sharpe 差 ≤ 25%）必须用**绝对值**比——「均值 OOS Sharpe」vs「IS Sharpe」。不要把「相对基线的增量 dSh」拿去比 IS Sharpe（会算出荒谬的 80%+ 而误判过拟合）。

### 参数生效方式（决定改动何时起作用）

| 类型 | 参数 | 生效 |
|---|---|---|
| **热读**（引擎每次调用 `STRATEGY_PARAMS.get`） | `momentum_top_n` / `momentum_lookback` / `momentum_rank` / `momentum_scope` / `max_positions` / `exit_mode` / `hard_stop_pct` / `risk_per_trade` / `stop_loss` / `require_daily_data` | **无需重启**，下次调用即生效 |
| **缓存**（引擎 `__init__` 读取） | `regime_mode` / `enable_rotation` / `rotation_*` | **需重启引擎**；自动化不得代为重启，须提示用户 |

这正是上午周期（11:40 午休）落盘热读参数后，**13:00 下午开盘即可生效并在当天被真实数据检验**的原因。

## 五、事实来源（先读，再动手）

| 文件 | 内容 |
|---|---|
| `logs/evolution_ledger.jsonl` | 逐日**真实**账户表现（跨日口径含隔夜重估），机器可读 |
| `reports/EVOLUTION_LEDGER.md` | 同上的人读表格 |
| `reports/EVOLUTION_DECISIONS.md` | 每次迭代的 IS/OOS 结果、通过/否决结论、实际落盘的参数改动 |
| `reports/OPTIMIZATION_BACKLOG.md` | 盘后复盘累积发现（P0>P1>P2），**仅追加不改写** |
| `config/settings.py` | 当前生效的 `STRATEGY_PARAMS` / `RISK_PARAMS` |

## 六、当前生产铁律（2026-09-16 收紧后）

| 参数 | 值 | 说明 |
|---|---|---|
| `EXIT_MODE` | `trend` | 趋势骑行退出（保留已验证 alpha） |
| `momentum_top_n` | **3** | **2026-09-17 EVOLVE 由 6 收紧**：IS Sharpe 1.27→1.35、MDD -34.29%→-24.00%；OOS 4 套折均值 Sharpe 增量 +0.216、均值 MDD -19.74%→-13.86%。机理：前 6 名中 4~6 名是「动量尾巴」，收益低却担同等尾部风险。**集中最强 3 只** |
| `max_positions` | 5 | 总持仓上限（top_n=3 后实际持仓 ≤3，本值仅作上限，勿再压到 3：已证伪，IS -83pt） |
| `max_drawdown_pct` | **−0.20** | 原 −0.25 低于回测 MDD −19.85%，形同虚设；收紧后更早介入 |
| `dd_recover_days` | **3** | 回撤熔断冷却恢复 |
| `halt_recover_days` | 1 | 连亏/日亏熔断冷却恢复（可恢复断路器） |
| `daily_stop_flatten_pct` | **−0.06（新增）** | 日内硬止损 + 强平全部可卖持仓（T+1 仍生效，每日仅一次） |
| `risk_per_trade` | **0.012** | 单笔风险（原 0.02） |
| `max_position_amount` / `max_order_amount` | **18 万** | 单标的/单笔上限（原 30 万） |
| `regime_mode` | `off` | 已证伪（7/7 折均跑输） |
| `enable_rotation` | `True` | 板块轮换 |
| `EXECUTION_MODE` | `paper` | 严禁切 live |
| `manual_entry_codes` | **5 只（观察篮）** | **2026-09-18 新增**：绕过动量/日线闸门与评分阈值建仓，用于把 LLM 排序前五真正拿在手里观察（paper 评估）。**不绕过**风控熔断/现金夹紧/max_positions/单标的金额上限。清单**热读**（改清单无需重启）；置空列表即完全恢复策略原行为 |
| `manual_entry_exit_exempt` | `True` | 观察篮仓位豁免「趋势破位/超时/单日暴跌」退出，**仅保留 −18% 硬止损**。必须开：这些标的多在 MA60 下方，否则建仓后下一轮即被破位卖出 → 买卖空转 |
| `min_daily_bias` | **2.0（2026-09-22 PM 由 0.2 收紧）** | 日线偏置闸门 `trend_up or bias>=该值`（trend_strategy.py:151）的第二路。bias∈[-1,1]，**2.0 = 关闭该通道**。回测历史从未建模 bias 通道（行为等价于 2.0），生产却一直开 0.2 → 口径背离且从未被验证。本轮补齐建模后验证：90×6 网格单调恶化（0.2 −0.238 / −0.3 −0.245 / −0.4 −0.295 / −1.0 −0.320），4 窗口共识下 0.2 **全负**（−0.298/−0.197/−0.238/−0.117，最差 −0.298）。IS ret 138.0%→159.0%、Sh 1.15→1.25、MDD −19.70%→−16.08%；OOS 均值 Sh 0.960→1.173、均值 MDD −10.89%→−9.24%。**缓存型参数（`self.p = dict(STRATEGY_PARAMS)` 构造期快照），改动需重启引擎生效**。可逆：改回 0.2 |
| **连亏降仓「僵尸冻结」自愈** | **2026-09-21 新增** | 坑：`_consec_loss≥5` 会把 `position_scale` 压到 **0.0**，`_handle_buy` 在 `scale<=0` **无声 return**（不产生任何拒绝日志）；`_halted` 不持久化、`_consec_loss` 却从 DB 恢复 → 重启后「未熔断却永久无法开仓」。已加 `_maybe_recover_zombie()`（**不重置 peak_asset**，回撤保护不弱化）。**冷却以「连亏起算日」判定**（`engine_state.risk_state` 持久化）——**不可用 `_halt_day`**，它不持久化且每次 `__init__` 重置为当天，用它会让自愈永远触发不了。**生效需重启引擎** |

> 参数禁区已于 2026-09-16 由用户（OWNER）明确撤销：不再存在"绝不自改参数"的限制，但**任何改动必须走上面的晋升闸门**，以样本外证据为准绳，不得凭直觉调参。风险底线（断路器有效、单笔风险上限、T+1、paper）是防裸奔，不是禁区。

## 七、运行环境与运维

- **解释器**：`C:\Users\lisinan\.conda\envs\qmt\python.exe`（其他解释器会缺 xtquant/sqlite3）
- **PATH 前置**：`...\qmt\Library\bin` 与 `...\qmt\DLLs`（sqlite3 DLL 依赖）
- **交易引擎**：`python main.py --web`（端口 5000），需在可达 xtdata 的**桌面持久会话**运行
- **miniQMT**：`C:\pazq_qmt\bin.x64\XtItClient.exe`（用户手动启动 + 勾自动登录）
- **守护**：`scripts/guardian.py`（5min 轮询）+ `scripts/health_check.py`（存活检测/自启动）
- **单实例锁**：`logs/engine.pid`；2026-09-16 修复 `main.py::_pid_alive` 致命 bug（原用 `OpenProcess` 单判据，会把强杀后句柄未回收的死进程误判存活 → 永久拒绝启动、守护反复拉起却起不来），现改用 `GetExitCodeProcess(STILL_ACTIVE=259)` 判定
| `rotation_require_daily_gate` | **True（2026-09-23 21:20 OWNER 授权启用）** | 轮动建仓须通过与主信号路径同一日线闸门 `trend_up or bias>=min_daily_bias`。**背景（真实缺陷）**：`_maybe_rotate` 两条分支（event_engine.py 补强空槽 / 弱换强）只要 `is_breakout`（日内涨幅≥1.5%）就无视 `daily-gate` 的 HOLD 直接买入，绕过了 09-22 落盘的 `min_daily_bias`；而 `backtest_daily.py` **无任何 rotation 逻辑**（grep 零命中）⇒ 第二条「实盘有、回测无」口径背离。实盘铁证：2026-09-23 10:00:52 以 `bias=-0.30 trend_up=False` 买入 688008.SH，3 秒后即被「趋势破位离场」判定卖出（T+1 锁住）。**回测**：代理字段 `breakout_bypass_gate` 90×6 单调剂量反应 0.5 −0.208 / 1.0 −0.187 / **1.5 −0.164** / 2.5 −0.038 / 3.0 −0.046 / 99(关闭) 0.000；4 窗口共识 1.5 **全负**（−0.083/−0.063/−0.164/−0.038）。**★ 启用依据（缺陷修复通道）**：OWNER 授权增设 `--track defect` 后，以「实盘现状 G1」为**反向对照**重跑（`--baseline "G1_轮动绕闸门_1.5"`），修复收益 60×9 +0.083 / 75×7 +0.063 / 90×6 +0.164 / 120×4 +0.038 → 均值 **+0.087 ≥ 0**、最差 **+0.038 ≥ −0.05** ⇒ 闸门②「方向不劣化」PASS，①IS↑ PASS（ret 128.6→159.0%、Sh 1.10→1.25），③④⑤⑥⑦⑧ 全 PASS，tests 239/0/239 ⇒ **启用**。**不会空仓**：只拦 `trend_up=False 且 bias<2.0` 的弱候选，`trend_up=True` 仍放行。**热读参数**：无需重启，下一轮动评估周期（120s 节流）生效 ⇒ 次日开盘生效。可逆：置回 False |
