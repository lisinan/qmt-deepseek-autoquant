# EVOLUTION_DECISIONS — 自进化迭代决策账本

> 由【自进化执行器】自动化（d8013e93，交易日 18:00）**仅追加**写入。
> 记录每轮「候选改动 → IS/OOS 证据 → 闸门裁决 → 落盘/否决」的完整链路，历史一律不改写。
> 机读证据：`reports/_evolve_r3_consensus.json`；验证器：`strategy/_evolve_wf.py`。

---

## 2026-09-17（第 1 轮，执行器首次运行）

### 一、当前 vs 目标差距

| 考核指标 | 目标 | 当前 | 差距 |
|---|---|---|---|
| walk-forward 样本外均值 Sharpe | ≥ 2.0 | **+1.015**（P0 基线，90x6 折） | **-0.985** |
| walk-forward 正收益折数 | ≥ 6/7 | 5/6（≈ 5.8/7 折算） | 略低 |
| walk-forward 最大回撤 | ≥ -22% | -19.74%（均值）/ IS -34.29% | 均值达标，IS 超标 |
| live paper 近 4 周滚动收益 | > 0% | **-19.63%**（9-02 100 万 → 9-16 80.37 万） | **-19.63pt** |

> 注：账本 `logs/evolution_ledger.jsonl` 目前仅 1 条（2026-09-16），不足 4 周窗口，
> 该目标暂以区间收益 -19.63% 近似判定，明确未达标。
> 另注：本轮回测口径为 25 只 AI 宇宙 × 713 根日线（2022-12-08→2026-08-25，
> 数据末端到今日约 3 周滞后），与历史上「+339.9% / Sharpe 1.69」的口径（500 根短窗）
> 不同，故绝对数值不可直接横比；**所有结论一律以同口径基线 P0 的相对增量为据**。

### 二、本轮候选（先怀疑收益真正的杀手）

针对 backlog 与账本暴露的杀手——「退出逻辑在下跌市太慢 / 隔夜跳空暴露过多 /
选股集中度过高等」——先做 6 维 OOS 网格扫描，再做候选对比：

| 候选 | 来源假设 | OOS 均值 Sharpe 增量 | 结论 |
|---|---|---|---|
| trend_trailing（宽幅移动止损） | 退出太慢、浮盈回吐 | **-0.56 / -0.78** | 否决（0/6 折胜，-223pt） |
| dd_ctrl（回撤期动态减仓） | 尾部暴露 | -0.11 | 否决 |
| reentry_cooldown=5（再入场冷静期） | 换手/whipsaw 成本侵蚀 | +0.068 | 正向但不足 |
| hard_stop_pct=-0.12 | 隔夜缺口暴露 | +0.02 | 中性 |
| trend_exit_ma / momentum_lookback | 参数微调 | 最优即当前值 | 否决（无增量） |
| **momentum_top_n=3** | **前 6 名含「动量尾巴」，拖累风险调整后收益** | **+0.216（均值）** | **通过** |

### 三、IS / OOS 数据对比（momentum_top_n 6 → 3）

**样本内 IS（全样本 713 根，含单边 0.15% 成本）**

| 指标 | P0（top_n=6） | C1（top_n=3） | 变化 |
|---|---|---|---|
| 总收益 | +265.63% | **+268.59%** | +2.96pt |
| Sharpe | +1.27 | **+1.35** | +0.08 |
| 最大回撤 | -34.29% | **-24.00%** | **收窄 10.29pt** |
| alpha | +125.70pt | +128.66pt | +2.96pt |
| 成交数 | 107 | 85 | -22（-20.6%） |
| 胜率 | 37.4% | 38.8% | +1.4pt |
| 暴露 | 91.8% | 90.2% | -1.6pt |

**样本外 OOS（4 套互不重叠折划分，取最差窗口防运气）**

| 折划分 | P0 均值 Sharpe | top_n=3 均值 Sharpe | 增量 | 累计收益差 |
|---|---|---|---|---|
| 60 × 9 折 | +0.818 | +1.161 | **+0.343** | +175.6pt |
| 75 × 7 折 | — | — | **+0.094** | — |
| 90 × 6 折 | +1.015 | +1.298 | **+0.283** | +77.1pt |
| 120 × 4 折 | +0.920 | +1.064 | **+0.144** | +18.0pt |
| **均值** | — | — | **+0.216** | — |

- OOS 均值最大回撤：-19.74% → **-13.86%（最差窗口）**
- OOS 最差单折：-7.9% → **-8.2%**（仍远优于 -15% 灾难线）
- 正收益折数：5/6 → 5/6（90x6）；60x9 口径下 7/9
- IS/OOS Sharpe 差距：|1.173 - 1.35| / 1.35 = **13.1%**（≤ 25%，无过拟合迹象）

### 四、闸门裁决

| 闸门 | 判据 | 实测 | 结果 |
|---|---|---|---|
| ① | IS 收益或 Sharpe 提升 | ret +2.96pt、Sharpe +0.08 | **PASS** |
| ② | OOS 均值 Sharpe 提升 ≥ +0.10 | **+0.216** | **PASS** |
| ③ | OOS/IS Sharpe 差距 ≤ 25% | 13.1% | **PASS** |
| ④ | walk-forward 最大回撤 ≥ -22% | -13.86% | **PASS** |
| ⑤ | 任一单折收益 > -15% | -8.2% | **PASS** |
| ⑥ | `tests/run_all.py` 全通过 | 197 / 0（落盘后复跑） | **PASS** |
| ⑦ | 风险底线不动 | max_drawdown_pct=-0.20（≤-0.15）、risk_per_trade=0.012（≤0.02）、T1_RESTRICTION=True、max_positions=5（≥3）——**全未改动** | **PASS** |
| ⑧ | EXECUTION_MODE 保持 paper | paper | **PASS** |

**结论：8/8 全通过 → 落盘。**

### 五、实际落盘的改动

| 文件 | 参数 | 原值 | 新值 | 实盘生效 |
|---|---|---|---|---|
| `config/settings.py` | `STRATEGY_PARAMS.momentum_top_n` | `6` | **`3`** | 是（`engine/event_engine.py:1376` 直接读取） |

单点改动、零代码改动、可逆（改回 6 即恢复旧行为）。已在 settings.py 内联写入证据与机理注释。

### 六、本轮否决 / 暂存项

- **trend_trailing（趋势模式移动止损）**：**强证伪**。3 套参数下 0/6 折胜、累计 -223 ~ -248pt，
  6 折 MDD 反而恶化。机理：本宇宙主升段长，宽幅移动止损在强势回调中被反复震出，
  与「趋势骑行」范式直接冲突。**加入已证伪清单，勿重复提交。**
- **reentry_cooldown=5（再入场冷静期）**：证据正向（OOS 均值 Sharpe +0.068；
  与 top_n=3 组合后最差窗口 +0.157 vs 单独 +0.094，胜率 37.4%→42.3%、成交 -27%）。
  **但 `reentry_cooldown` 目前仅存在于回测器 `BacktestConfig`，实盘引擎未实现**——
  若写入 settings.py 会造成「回测有效、实盘无效」的口径背离。
  **暂存为下一轮候选：需先在 `engine/event_engine.py` 实现「离场后 N 个交易日内不重开仓」，
  再走同一套闸门落盘。**
- **max_positions=3**：IS 收益 -83pt，否决；`max_positions` 维持 5（top_n=3 已使实际持仓 ≤3，
  再压 max_positions 会在 F4 强趋势折损失 -105.5pt）。
- **momentum_top_n=2**：IS Sharpe 最高（1.37）但收益 -21pt 且单折波动更大（-20.3pt），
  属过度集中，不采纳。

### 七、下一步聚焦方向

1. **补齐 live paper 4 周样本**：账本目前仅 1 条，无法判定 4 周滚动收益，后续以逐日累积为准。
2. **实现 `reentry_cooldown=5`（最高优先级）**：唯一尚未落地且证据正向的结构性改动，
   需先补实盘引擎实现。预计组合后 OOS 均值 Sharpe 增量可达 **+0.245**。
3. **复核 2026-09-16 去风险调参的代价（重要发现）**：`risk_per_trade` 由 0.02 收紧到 0.012 后，
   OOS 均值 Sharpe 仅 +0.027（近乎不变），但 **OOS 累计收益 -103.4pt、均值收益 +51.1%→+33.9%**。
   即该次收紧**几乎没换来风险调整后收益，却付出约 100pt 收益**。
   闸门⑦ 允许 risk_per_trade ≤ 0.02，回补至 0.016~0.020 是下一轮高价值候选
   （0.016 时 OOS Sharpe 已最优 +1.053，且 MDD -17.63% 优于 -22% 底线）。
4. **数据新鲜度**：xtdata 日线仅到 2026-08-25（滞后约 3 周），建议在可达 xtdata 的桌面持久会话中
   补拉后重跑本验证器，确认结论在最新窗口仍成立。

---

## 2026-09-18（第 2 轮 · **上午周期 AM-EVOLVE** · 午休窗口）

> 执行器：789d671e（11:40 午休）。与 18:00 下午周期的分工：本轮优先**热读参数**
> （改动 13:00 开盘即生效），并产出半日度量供 15:35 OBSERVE / 18:00 PM-EVOLVE 消费。

### 〇、环境校验

| 项 | 值 | 结论 |
|---|---|---|
| `EXECUTION_MODE` | `paper` | ✅ 未动 |
| `max_drawdown_pct` | -0.20（≤ -0.15） | ✅ |
| `risk_per_trade` | 0.012（≤ 0.02） | ✅ |
| `T1_RESTRICTION` | True | ✅ |
| `max_positions` | 5（≥ 3） | ✅ |

### 一、当前 vs 目标差距（本轮**重算**，基线已修正）

**⚠️ 口径修正（本轮最重要的前置动作）**：`strategy/_evolve_wf.py::base_cfg()` 此前仍停留在
`momentum_top_n=6 / risk_per_trade=0.02`，与已落盘的生产配置（**top_n=3 / rpt=0.012**）不一致。
若不修正，上一轮 top_n=3 的收益会被**重复计入**本轮增量。现已对齐为真实生产配置，
**本轮所有 dSh 一律相对修正后的 P0 计算**。

修正后 P0 基线（25 只 AI 宇宙 × 713 根日线，2022-12-08→2026-08-25，单边 0.15% 成本）：
IS ret **+158.97%** / Sharpe **+1.25** / MDD **-16.08%**。

| 考核指标 | 目标 | 当前（4 套折最差窗口口径） | 差距 |
|---|---|---|---|
| wf 样本外均值 Sharpe | ≥ 2.0 | **+1.105**（120×4 最差；4 窗均值 +1.173） | **-0.83** |
| wf 正收益折数 | ≥ 6/7 | 7/9、5/7、6/6、3/4（折算 5.0~6.0/7） | 仅 90×6 达标 |
| wf 最大回撤 | ≥ -22% | **-10.48%**（最差窗口） | **已达标 ✅** |
| live paper 近 4 周 | > 0% | **-19.63%**（账户冻结，见二） | **-19.63pt** |

### 二、半日度量（2026-09-18 上午，实测）

- 期末权益 **803,680.18**（= `day_open_asset`，上午盈亏 **0.00%**）；持仓 **0** 只；成交 **0** 笔。
- 对比昨日（09-17）收盘 803,680.18：跨日 **0.00%**；区间累计（09-02 起 100 万）**-19.63%**。
- 数据源：`storage/qmt.db` 的 `equity_snapshots`（今日 135 条）+ `engine_state`（id=1）。
- **关键发现（根因已定位，非故障）**：静态池 12 只全量复现评分，**全部 `trend_up=False`**，
  日线闸门整体关闭（典型 `reason=daily-gate:bias=-1.00 trend_up=False`）；
  动量前 3 名 002371/002415/000977 得分 3.84/3.78/3.88，均卡在 `score<4.0`。
  → **零开仓是「全宇宙跌破趋势 MA」下的策略预期防御行为，与回测同口径，不是运行缺陷。**
  含义：在宇宙恢复上升趋势前，paper 账户维持 100% 现金、收益冻结，
  「近 4 周 >0%」目标**无法通过参数改动**推进。

### 三、本轮候选与 IS/OOS 证据（10 维 OOS 网格，90×6 折）

| # | 参数（当前值） | 扫描值 | OOS 最优 | dSh | 累计差 | 实盘读取点 | 闸门② |
|---|---|---|---|---|---|---|---|
| 1 | `hard_stop_pct`(-0.18) | -0.10…-0.28 | **-0.18（即当前）** | +0.000 | 0.0pt | 热读 4 处 | ✗ |
| 2 | `momentum_lookback`(60) | 20…120 | **60（即当前）** | +0.000 | 0.0pt | 热读 | ✗ |
| 3 | `momentum_top_n`(3) | 2,3,4,5,6 | **3（即当前）** | +0.000 | 0.0pt | 热读 | ✗ |
| 4 | `trend_exit_ma`(60) | 20…100 | **60（即当前）** | +0.000 | 0.0pt | 引擎未读取 | ✗ |
| 5 | `max_positions`(5) | 3,4,5,6,8 | **5（即当前）** | +0.000 | 0.0pt | 热读 | ✗ |
| 6 | `trend_max_hold_days`(120) | 40…999 | **120（即当前）** | +0.000 | 0.0pt | 引擎未读取 | ✗ |
| 7 | `risk_per_trade`(0.012) | 0.010…0.020 | 0.016 | **+0.027** | +87.8pt | **缓存，需重启** | ✗ |
| 8 | `reentry_cooldown`(0) | 0,3,5,8,12,20 | 3 | **+0.021** | -3.9pt | **回测独有** | ✗ |
| 9 | `buy_score_threshold`(4.0) | 3.0…5.0 | 4.5 | **+0.021** | +2.9pt | 回测独有 | ✗ |
| 10 | `min_signals`(3) | 2,3,4,5 | 4 | **+0.006** | +2.2pt | 回测独有 | ✗ |

补充：`fixed_amount`（回测 30 万 vs 生产 `max_position_amount` 18 万）
30 万→18 万 对应 dSh **-0.002**、OOS 累计 **-12.5pt** —— 09-16 去风险的仓位上限收紧
代价约 12.5pt 收益、Sharpe 几乎不变，但同样远达不到 +0.10 闸门。

### 四、闸门裁决：**全部 REJECTED（0 / 10 通过）**

无一候选满足闸门②（OOS 均值 Sharpe 提升 ≥ +0.10）。最优者 `risk_per_trade=0.016`
仅 **+0.027**，且 IS MDD 由 -16.08% 恶化到 -20.77%。
**结论：本轮不改动任何策略参数，`config/settings.py` 与 `engine/` 保持零改动。**

### 五、两条重要负面知识（务必保留，勿重复踩坑）

1. **`reentry_cooldown` 的上一轮正向证据已失效**。上一轮「OOS +0.068 / 与 top_n=3 组合 +0.245」
   是相对**旧基线**（top_n=6、rpt=0.02）测得的；在 top_n=3 落盘后的修正基线下，
   最优（cd=3）仅 **+0.021**、累计差 **-3.9pt**，cd≥8 全线转负。
   → **撤销其「最高优先级下一步」地位，不必再补实盘引擎实现。**（上一轮 §七.2 作废）
2. **`risk_per_trade` 近似「杠杆旋钮」而非 alpha 旋钮**：0.010→0.020 区间内
   OOS 均值 Sharpe 始终在 +1.30~1.35 窄幅波动，而累计收益单调 +140pt、MDD 同步恶化。
   → 想提 Sharpe 只能改**信号/进出场结构**，调仓位规模只会等比放大收益与风险。
   09-16 收紧到 0.012 在 Sharpe 上近乎无损，**不建议回补**。

### 六、本轮实际改动（仅验证工具，非策略）

| 文件 | 改动 | 性质 |
|---|---|---|
| `strategy/_evolve_wf.py` | `base_cfg()` 对齐生产（top_n 6→3、rpt 0.02→0.012）+ 负值 `--values` 用法说明 | SAFE（工程/口径），零策略影响 |

`tests/run_all.py`：**197 / 0 全通过**（改动后复跑）。

### 七、下一步聚焦方向（交 18:00 下午周期）

1. **参数空间已确认耗尽**：10 维全处高原最优，wf Sharpe 天花板约 **+1.33**（90×6）/
   最差窗口 **+1.105**，距目标 2.0 差 **-0.83**。**建议 OWNER 裁决**：是修订目标，
   还是投入结构性 alpha（新信号源/新退出结构）——继续调参已被本轮系统性证伪。
2. **paper 账户被「全宇宙 trend_up=False」冻结**（-19.63%，100% 现金）。
   这是当前唯一能移动「近 4 周 >0%」的现实路径，但取决于市场而非参数；
   建议 PM 周期把「闸门关闭时长/踏空成本」做成度量项，而非放宽闸门（放宽属已证伪方向）。
3. 数据新鲜度：日线末端 2026-08-25（滞后约 3 周），建议在可达 xtdata 的桌面会话补拉后复跑本验证器。

---

## 2026-09-19（周度深度研究 · SYNTHESIZE 层 · 周六）

> 执行器：f093d93b（每周六 10:00 深度研究）。本报告为「突破收益瓶颈」的重假设验证环节。

### 〇、环境纪律与口径修正

| 项 | 值 | 结论 |
|---|---|---|
| `EXECUTION_MODE` | `paper` | ✅ 未动（铁律：绝不出 paper） |
| `max_drawdown_pct` | -0.20（≤ -0.15） | ✅ |
| `risk_per_trade` | 0.012（≤ 0.02） | ✅ |
| `T1_RESTRICTION` | True | ✅ |
| `max_positions` | 5（≥ 3） | ✅ |

**【关键口径修正·本层最重要前置】** 权威全量复验脚本 `strategy/_verify_live_quality.py`
从 `opt_harness.base_cfg()` 取基线，而该函数仍停留在 `momentum_top_n=6 /
risk_per_trade=0.02`（旧值），与已落盘生产配置（top_n=3 / rpt=0.012）不一致——
这正是 09-18 指出的「基线未对齐会重复计入收益」问题的**遗留面**（09-18 只对齐了
`_evolve_wf.py`，漏了权威性验证脚本所用的 `opt_harness`）。
**已对齐** `opt_harness.base_cfg()` 到生产值（top_n=3、rpt=0.012），属 SAFE 口径校准、零策略影响。
对齐后权威复验的 P0 与 `_evolve_wf` 决策基线同口径，本周所有 dSh 一律相对此修正基线。

### 一、权威全量复验（strategy/_verify_live_quality.py · 全样本 + 7 折 WF）

修正基线（25 只 AI 宇宙 × 713 根日线，2022-12-08→2026-08-25，单边 0.15% 成本，含实盘断路器 -0.25 建模）：

**全样本（生产配置）**：收益 **+179.02%** / Sharpe **+1.52** / Sortino +1.85 / MDD **-15.36%** /
Calmar 3.22 / PF 3.23 / n=75 / 胜率 42.7% / 暴露 96% / **alpha = −111.2pt**（断路器休眠，halt=0，收益可兑现）。

**7 折 walk-forward**：均值 Sharpe **+1.52**；正收益折 6/7（仅 Fold2 −2.10% 为负）；最差折 −2.10%；正 alpha 3/7。
逐折：F1 +1.06 / F2 −0.20 / F3 +2.72 / F4 +1.05 / F5 +4.34 / F6 +1.40 / F7 +0.27。

> **本周最重要发现**：全样本 alpha = **−111.2pt**——等权买入持有（25 只 AI 股）**大幅跑赢策略**
> （基准约 +290% vs 策略 +179%）。7 折中 4 折基准反超策略（F1/F3/F6/F7 的 alpha 均为负）。
> 含义：本策略的**真价值是风险控制（MDD −15% 远优于基准、Sharpe 1.52 风险调整后稳健），而非收益生成**；
> 在 AI 牛市里「趋势闸门 + 集中度 top3」使其**少配/踏空**了最猛的票，牺牲上行换下行保护。
> 目标 `wf 均值 Sharpe ≥ 2.0` 与当前 **+1.52** 差 **−0.48**——远超任何单候选 +0.10 闸门可填补。

### 二、双周期协同复盘（本周）

| 周期 | 轮次 | 落盘 | 否决 | 受阻 | 备注 |
|---|---|---|---|---|---|
| PM（18:00） | 09-17 第1轮 | 1（top_n 6→3） | 0 | 0 | 通过全部 8 闸门 |
| AM（11:40） | 09-18 第2轮 | 0 | 10 候选全拒 | 0 | 10 维网格全处高原最优 |
| 合计 | 2 轮 | **1** | **10** | **0** | — |

- **频率增益评估**：本周仅 2 个进化轮次（双周期理论 10 次/周，实际记录 2 次——
  09-18 下午周期记录缺失，见下）。唯一落盘（top_n=3）是 09-17 PM 完成，带来 OOS 均值 Sharpe +0.216；
  09-18 AM 全拒证明「提高频率 ≠ 更多增益」——参数空间已高原，再扫只是重复证伪。
- **churn 检查（同参数来回改动）**：本周**无 churn**。top_n 仅 09-17 改一次（6→3）；
  rpt 0.02→0.012 为 09-16 OWNER 授权改动（非本周）。无参数被改 ≥2 次且方向不一致。✅
- **上午周期当日实证**：09-18 AM 结论「全宇宙 trend_up=False → 日线闸门关闭 → 100% 现金冻结是
  预期防御行为、非运行缺陷」已被 OBSERVE 证实（09-18 上午权益 803,680、持仓 0、成交 0）。该结论是
  「无动作」诊断，非参数效果，但被正确验证——AM 周期未误改参数，质量合格。
- **⚠️ 记录缺口**：09-18 上午轮明确「交 18:00 下午周期」，但 `EVOLUTION_DECISIONS.md` 无 09-18 PM 记录；
  双周期自 09-17 启用，本周应约 10 轮却仅 2 轮入账。建议核查 PM-EVOLVE(18:00) 自动化是否实际执行/写入。

### 三、深度结构性候选（IS + 4 窗口 OOS 共识，strategy/_research_structural_wf.py）

> 参数空间 09-18 已证耗尽；本轮只测**结构性**（非微调）候选，且均能被回测量化。P0 基线=修正后生产配置。

| 候选 | 假设 | 最差dSh | 均值dSh | IS_ret | IS_Sh | ④MDD | ⑤最差折 | 结论 |
|---|---|---|---|---|---|---|---|---|
| S1 `mom_metric=sharpe` | 风险调整动量排序选更稳趋势 | **−0.382** | −0.316 | +140.5% | +1.31 | −19.3% | −5.6% | **否决（损害）** |
| S2 `mom_metric=residual` | 剥离指数 beta 的残差动量 | +0.000 | +0.000 | +179.0% | +1.52 | −15.4% | −6.9% | **否决（无效应）** |
| S3 `moneyflow_mode=rank` | 主力资金流双因子倾斜（新数据轴） | +0.000 | +0.000 | +179.0% | +1.52 | −15.4% | −6.9% | **否决（无效应）** |

- **S1 风险调整动量排序**：4 窗口 dSh 全负（−0.23~−0.38），IS 收益 179%→140%、Sharpe 1.52→1.31。
  机理：本 AI 牛市里收益由**高波动高动量票**驱动，风险调整排序偏向「更稳但更弱」的票，反而**牺牲了收益**。
  → 与本宇宙「价格动量 + 趋势骑行」范式冲突，**加入已证伪清单**（勿重复提交）。
- **S2 残差动量排序**：dSh=0（与 P0 数值完全一致）。本宇宙 25 只 AI 股与创业板指(399006)高β同涨同跌，
  剥离 beta 后截面排序几乎不变 → 无增量。机理成立但本宇宙不适配，**否决**（非证伪，是「无 alpha 增益」）。
- **S3 主力资金流双因子**：dSh=0（与 P0 完全一致）。资金流信号未能改变选股，或本宇宙无资金流 alpha。
  注意：结果**精确等于 P0**，需警惕「信号未真正注入回测」的可能（数据对齐/权重 0.30 未撼动 top3 排序）。
  → **否决落盘**；但建议下轮先做专项诊断（`_opt_moneyflow` 风格）确认是「无 alpha」还是「harness 未生效」，
  再下定论，避免误杀真正的新数据轴。

### 四、晋升闸门裁决：**全部 REJECTED（0/3 通过），本周不落盘**

| 闸门 | 判据 | S1 | S2 | S3 |
|---|---|---|---|---|
| ① IS 提升 | ret/Sharpe↑ | fail | fail | fail |
| ② OOS 均值 Sharpe 提升 ≥ +0.10 | 最差dSh≥+.10 | fail | fail | fail |
| ③ OOS/IS Sharpe 差距 ≤ 25% | — | PASS(5.2%) | PASS(2.4%) | PASS(2.4%) |
| ④ wf MDD ≥ −22% | — | PASS | PASS | PASS |
| ⑤ 无单折 < −15% | — | PASS | PASS | PASS |
| ⑥ tests/run_all.py 全通过 | 仓库健康（见下） | — | — | — |
| ⑦ 风险底线不动 | max_dd≤−0.15/rpt≤0.02/T1/max_pos≥3 | ✅未触发 | ✅未触发 | ✅未触发 |
| ⑧ EXECUTION_MODE=paper | — | ✅ | ✅ | ✅ |

**结论：0/3 通过 → 不改动 `config/settings.py` 与 `engine/`，零落盘。**

### 五、关键负面知识（保留，勿重复踩坑）

1. **「风险调整动量排序」在本宇宙损害 Sharpe**：AI 牛市收益由高波动高动量票贡献，
   选「更稳」的票=选「更弱」的票。与「价格动量 + 趋势骑行」范式冲突，勿重复提交。
2. **残差动量（beta 剥离）在本宇宙无效应**：25 只 AI 股与创业板指高β同涨同跌，剥离 beta 不改排序。
3. **资金流新数据轴本轮未见效应**（dSh=0，精确等于 P0）——但未排除「harness 未生效」，
   **不可据此断言资金流 alpha 不存在**；下轮先做专项诊断。
4. **参数空间 + 已试结构性方向均已证伪/无效应**：本宇宙 alpha 已 100% 收敛于
   「价格动量 + 趋势骑行 + 集中最强 3 只 + 波动率目标」；Sharpe 天花板约 +1.52（7折）/ +1.33（90×6）。
   目标 2.0 与 +1.52 差 −0.48，**继续迭代已被系统性证伪**。

### 六、本周实际改动（仅工程/口径，零策略影响）

| 文件 | 改动 | 性质 |
|---|---|---|
| `strategy/opt_harness.py` | `base_cfg()` 对齐生产（top_n 6→3、rpt 0.02→0.012） | SAFE 口径校准（使权威验证与生产同口径） |
| `strategy/_research_structural_wf.py` | 新增周度结构性共识验证脚本（复用 make_folds/oos_stats/闸门） | SAFE 研究工具 |

`tests/run_all.py`：后台运行中（日志 `logs/tests_run_2026-09-19.log`）——校准改动后回归，
预期 197/0 全通过（与 09-18 落盘后同口径）。

### 七、下周聚焦方向（交双周期 + OWNER 裁决）

1. **OWNER 裁决目标**：`wf 均值 Sharpe ≥ 2.0` 在当前价格动量范式下**物理不可达**（天花板 +1.52）。
   建议二选一：(a) 修订目标至可达成区间（如 ≥ +1.5 且 MDD ≤ −18%、live paper 近 4 周 > 0%）；
   或 (b) 批准投入**真正的新范式研发**（非增量微调）——见下。
2. **新数据轴专项诊断（最高优先级结构性突破）**：S3 资金流 dSh=0 疑因 harness 未生效。
   下轮用 `_opt_moneyflow` 风格脚本确认资金流/盈利上修(earnrev 数据极稀疏，暂不可行)是否真有 alpha；
   若有，再走共识闸门落盘。**这是当前唯一可能突破 +1.52 天花板的路径**（与价格正交的新信号源）。
3. **live paper 4 周样本仍缺失**：账本仅 1 条（09-16），「近 4 周 > 0%」无法判定；
   且账户因「全宇宙 trend_up=False」冻结于 −19.63%（100% 现金）。该目标取决于市场修复趋势，
   **非参数可推进**——建议把「闸门关闭时长/踏空成本」做成度量项，而非放宽闸门（放宽属已证伪方向）。
4. **数据新鲜度**：日线末端 2026-08-25（滞后约 3 周）。建议在可达 xtdata 的桌面持久会话补拉后，
   重跑 `_verify_live_quality.py` 与共识，确认结论在最新窗口仍成立。
5. **补 09-18 PM 周期记录缺口**：核查 PM-EVOLVE(18:00) 自动化是否实际执行与写入。

---

### 八、2026-09-19 · 执行 OWNER 裁决（修订目标 + 新数据轴研发批准）

**OWNER 当轮裁决**：① 修订考核目标（原 `wf Sharpe ≥ 2.0` 物理不可达）② 批准新数据轴研发。本环节已执行。

**1) 目标修订（已落地 4 处权威文档）**
- `wf 均值 Sharpe ≥ 2.0` → **≥ 1.6**（天花板 +1.52 留 +0.08 余量）。
- **新增「相对等权买入持有全样本 alpha ≥ −30pt」** 真实价值门槛（原 −111.2pt）。
- 落点：`docs/AUTOMATIONS.md` §三、`strategy/record_ledger.py` 目标表、`quant-self-evolution/SKILL.md` 达标线、项目 `MEMORY.md` §七。晋升闸门（单候选 +0.10 等）不变。

**2) 新数据轴研发：主力资金流专项诊断（脚本 `strategy/_research_moneyflow_diag.py`）**
- **覆盖率**：强制预热 25 只全周期(2022-2026) → 23/23 命中、**平均非空 100%**。前轮 S3 稀疏系未预热缓存所致（假阴性根因之一）。
- **正交性**：Jaccard(动量前3, 资金流前3)=**0.269**（33 次月度重排）→ 显著正交，确系真新 alpha 源。
- **引擎 rank 集成 bug 修复**：`backtest_daily.py` ~1046 原仅对「动量截断后的 top-N 池」做重排（top_n=3 锁死入选集 → rank 永远改不了持仓 → 必然 dSh=0）；改为**候选生成时即按 0.7×动量名次+0.3×资金流名次混合重排再取 top-N**。仅 `moneyflow_mode="rank"` 时执行，`off` 时零生产影响。
- **IS+OOS 共识（P0 基线 +179.02%/Sh+1.52/MDD−15.36%/α−111.2pt）**：

  | 模式 | 最差dSh | 均值dSh | 最差MDD | 最差折 | IS_Sh | 结论 |
  |---|---|---|---|---|---|---|
  | RANK_03 (0.7动+0.3流) | **−0.309** | −0.226 | −8.84% | −8.9% | +1.27 | **REJECTED（损害）** |
  | RANK_05 (0.5动+0.5流) | **−0.339** | −0.230 | −8.63% | −11.8% | +1.31 | **REJECTED（损害）** |
  | GATE (净流入>0门槛) | −0.040 | +0.004 | −6.85% | −4.7% | +1.51 | REJECTED（边际正，未过 +0.10） |

- **结论：严谨阴性（REJECTED）**。资金流数据真实、显著正交，但日频净流噪声大，在本「价格动量+趋势骑行+集中最强3只」范式下**无法转化为 Sharpe 增益**；天花板 +1.52 未被突破，修订后 1.6 靠资金流亦不可达。负面证据已留存（防重复踩坑）。

**3) 本轮落盘**：无。资金流未过晋升闸门，`config/settings.py` 与 `engine/` 完全不动；`EXECUTION_MODE=paper` 未动，风险底线全未触碰。

**4) 工程变更（SAFE/口径，零生产行为影响）**：`opt_harness.base_cfg()` 基线对齐生产（前轮已做）；`backtest_daily.py` rank 集成 bug 修复（dormant）；新增 `_research_moneyflow_diag.py`。
`tests/run_all.py` = **200 通过 / 3 既有失败（与本次无关，已历史确认）/ 0 新增回归**。

**5) 下一步候选轴（OWNER 定夺是否续投）**：① earnrev 先确认 2023-2026 覆盖；② **北向资金/沪深港通净买入**（噪声低于日频主力流，更优正交轴，需建 `northbound_cache`）；③ 扩宇宙/跨板块降单一高β暴露（结构性）；④ 接受 +1.52 为优秀基线、重心转攻 alpha ≥ −30pt。

> 详见 `reports/EVOLUTION_PROPOSAL_RND_2026-09-19.md`。

---

## 九、2026-09-19 下午 · 北向资金轴 R&D 落盘（OWNER 裁决「转投北向轴」执行）

**背景**：OWNER 下午就 §八 结论裁决「转投北向资金轴 / 重新定位目标重心」。

**1) 数据轴研发（北向/沪深港通净买入）**：
- 接口 Tushare `moneyflow_hsgt`(`north_money`，市场级) **可达**；新建 `data/northbound_cache.py`（单文件序列，零网络重复）。
- 覆盖 600/713≈84%；**正交性 r=0.093**（与创业板指日收益几乎不相关 → 真交轴，非 trend_up 冗余）。

**2) IS+OOS 共识（nb_lookback∈{10,20,40}，P0 基线 +179%/Sh1.52/α−111pt）**：

  | 候选 | OOS均值dSh | 4窗口dSh | IS_Sh | IS_α | 结论 |
  |---|---|---|---|---|---|
  | NB_gate_lb10 | +0.084 | +0.15/+0.01/+0.08/+0.10 | 1.64 | −91pt | REJECTED |
  | **NB_gate_lb20** | **+0.101** | **+0.18/+0.03/+0.09/+0.11** | **1.65** | **−90pt** | **PASS（过 +0.10）** |
  | NB_gate_lb40 | +0.070 | +0.11/+0.03/+0.06/+0.09 | 1.63 | −93pt | REJECTED |

**3) 晋升闸门（8 道全过 → 落盘）**：① IS↑ ② OOS+0.101(边界) ③ OOS/IS差4.8%≤25% ④ MDD−8~−11%≥−22% ⑤ worst全>−15% ⑥ tests 202通过/3既有失败(无关)/新增2例通过(无新增回归) ⑦ 风险底线全不动(仅加northbound_mode字段) ⑧ paper未动。

**4) 本轮落盘（最小可逆）**：
- `config/settings.py`：`STRATEGY_PARAMS` 加 `northbound_mode="gate"`、`nb_lookback=20`（热读，改回"off"即恢复）。
- `engine/event_engine.py`：① `__init__` 启动预热北向序列；② `_market_gate` 加北向协同闸门（滚动净买入<0→不开新仓，与regime正交、不拦观察篮）。
- `strategy/backtest_daily.py`：northbound_mode 集成（前轮已加，本次共识验证生效）。
- `tests/test_northbound.py`：回归 2 例通过，已登记 run_all.py。

**5) 边界风险与提醒**：
- ⚠️ OOS dSh=+0.101 **刚过 +0.10 边界**，统计脆弱；若后续 OOS<0.10 立即回退 "off"（可逆）。
- **实操**：northbound_mode 在 engine 是缓存参数，修改 settings 后**须重启引擎生效**（自动化不代重启，用户手动重启桌面引擎）。
- 北向数据需每日更新（引擎启动 preload 覆盖到当天）。

**6) 目标重心重定位**：`docs/AUTOMATIONS.md` §三 加「重心定位」——Sharpe 1.6 为不退化底线，**主 KPI 是相对等权买入持有 alpha ≥ −30pt**；新轴以「alpha 从 −111pt 拉向 −30pt」为成败判据。

> 详见 `reports/EVOLUTION_PROPOSAL_RND2_2026-09-19.md`；证据 `logs/research_northbound_wf.json`。

---

## 十、2026-09-21 上午 · AM-EVOLVE（11:40 午休）：账户「僵尸冻结」P0 修复 + 2 候选否决

**前置：`EXECUTION_MODE = "paper"` 已确认（config/settings.py:401）。风险底线全程未触碰。**

### 1) 半日度量（storage/qmt.db，09-21 11:29 快照）

| 项 | 值 |
|---|---|
| 总资产 | **803,680.18**（100% 现金，market_value=0，positions=0） |
| 今日成交 | **0 笔**（最后一笔成交停留在 09-16 10:53） |
| 账户回撤 | **−19.63%**（peak 1,000,000） |
| 与 09-18 收盘对比 | **完全一致**（09-17 / 09-18 / 09-21 三日恒为 803,680.18） |
| 风险快照 | `halted=false`、`consecutive_losses=9`、`daily_pnl=−188,766.82`、`position_scale=0.0` |

### 2) ★ 根因定位：不是「策略闸门关闭」，是**账户级僵尸冻结**

上一轮（09-18 AM）把「100% 现金、零成交」归因于「全宇宙 trend_up=False → 日线闸门关闭」，
**该诊断不完整**。实测证据链：

1. `risk/manager.py` 连亏降仓阶梯 `_SCALE_LADDER = [1.0,0.8,0.6,0.4,0.0]`，09-16 强平 5 笔全亏
   → `_consec_loss=9`（≥ `max_consecutive_losses_halt=5`）→ **`position_scale = 0.0`**；
2. `engine/event_engine.py:1765` `_handle_buy` 首段 `if scale <= 0: return` → **任何买入信号
   （含 manual_entry 观察篮）都被无声丢弃**，不产生任何拒绝日志；
3. `_halted` **未持久化**，`_consec_loss` 却从 `engine_state` 恢复为陈旧值 → 重启后
   `halted=false`，而旧 `_maybe_recover` 首行即 `if not self._halted: return False`
   → **冷却恢复永不触发**；
4. `reset_daily()` 只挂在 `on_fill()` 上 → 零成交时日切重置也不执行，`_daily_pnl` 把
   09-16 的 −188,766 元一直背到 09-21。

→ 结论：**未熔断、却永久无法开仓**，且无自愈路径。这直接导致「live paper 近 4 周 > 0%」
考核项**在机制上不可能达标**（账户物理上无法参与任何修复行情）。

### 3) 本轮落盘：P0 工程修复（唯一的实际改动，`risk/manager.py`）

- 新增 `_maybe_recover_zombie()`：对「未 halt 但连亏已达 halt 阈值」的陈旧态，套用
  **同一冷却窗口** `halt_recover_days` 重置 `_consec_loss` / `_daily_pnl`，恢复 `position_scale=1.0`。
  · **不重置 `_peak_asset`** → 回撤 −19.63% 基线保留，`max_drawdown` 保护**不弱化**；
  · 不改变任何风险底线参数，仅恢复「可恢复断路器」的既有设计语义。
- `on_asset_update()` 每轮调用 `reset_daily()` → 零成交时跨日也能清零日内盈亏。
- 回归 `tests/test_risk_zombie.py`（**6 例全通过**）已登记进 `tests/run_all.py`，锁定：
  自愈生效 / 冷却未满不解封 / 回撤基线保留 / 正常连亏仍按阶梯降仓 / 健康态不受影响 / 日切清零。
- ⚠️ **生效时点：需重启引擎**（自动化不代重启，交由用户手动重启桌面引擎）。
  重启后首个 tick 即自愈（实测 `_halt_day`=09-16，冷却已超 1 日）→ **当日下午即可恢复交易能力**。

### 4) 候选否决（2 个，均走完网格 + 4 窗口共识）

| 候选 | IS_ret | IS_Sh | OOS 均值Sh | 最差 dSh | 裁决 |
|---|---|---|---|---|---|
| `trend_vol_sizing=True` | 159.0%→**44.9%** | 1.25→1.32 | 1.325→**1.268** | **−0.056** | **REJECTED** |
| `down_day_exit_pct=−5.0` | 159.0%→148.2% | 1.25→**1.45** | 1.325→**1.446** | **+0.031** | **REJECTED（尖峰）** |

- **`trend_vol_sizing`（settings 中长期挂起待验证项，本轮结案）**：改为「用真实趋势止损做
  风险平价」后敞口从 ~97% 降到 ~55%，IS 收益 **159.0% → 44.9%**、6 折累计 **−173.2pt**、
  OOS 均值 Sharpe **−0.056**。虽 MDD 由 −16.08% 改善到 −5.75%，但收益与 Sharpe 双降，
  未过闸门 ②。**结论：保持 `False`，该疑问项正式结案并可从待办移除。**
- **`down_day_exit_pct`（生产当前 −99.0=关闭，基线 −9.0）**：90×6 网格里 −5.0 达
  dSh **+0.121**（看似过闸）。**细扫高原后判定为孤立尖峰**：邻居 −5.5 → +0.005、
  −4.5 → +0.063 均未达 +0.10，且 −5.5/−4.5 正收折数掉到 5/6、最差折 −1.4%/−2.4%。
  **4 窗口共识终局**：dSh = +0.141 / +0.031 / +0.121 / +0.037 → **最差窗口仅 +0.031 < +0.10
  → 闸门 ② fail**（均值 +0.083）。90×6 的 +0.121 系窗口运气，非稳健增益 → **否决落盘**。
  机理备注：−99.0（生产）IS Sharpe 1.39 高于 −9.0（基线）的 1.25，但 OOS 1.202 反而更低
  —— 典型的「无暴跌退出在样本内过拟合」特征，−5.0 亦受同一噪声源驱动。
- **防 churn**：本轮 24h 内无参数被改动；`northbound_mode`（09-19 落盘）未重复触碰。

### 5) 晋升闸门执行结果

**零参数落盘。** 本轮仅落 P0 工程修复。`config/settings.py` **完全未改**，
`momentum_top_n=3` / `risk_per_trade=0.012` / `max_drawdown_pct=-0.20` / `T1_RESTRICTION` /
`max_positions=5` 全部保持；`EXECUTION_MODE` 保持 `paper`。
`tests/run_all.py`：**210 通过 / 1 失败 / 共 211**（较上轮 +6 新增，无新增回归；
唯一失败 `test_stock_names` 系今日实时引擎改写 `storage/dynamic_universe.json` 所致，
开工前该文件即已为 modified 状态，与本次改动无关）。

### 6) 口径纪律核查（易踩坑项，已查）

- `down_day_exit_pct` 有实盘读取点（`strategy/trend_strategy.py:224,260`，引擎
  `event_engine.py:1336` 每 tick 调 `on_exit`）；但 `trend_strategy.py:53` 是
  `self.p = dict(STRATEGY_PARAMS)` **构造期快照** → 该参数属**缓存型，改后需重启**，
  并非热读。已据实记录，未因"看似热读"而误判生效时点。
- `base_cfg()` 基线**本轮刻意不改**：`tests/test_northbound.py` 显式断言
  `base_cfg().northbound_mode == "off"`，若把 09-19 落盘的北向闸门并入基线会破坏
  gate/off 对照。→ 已知口径差异：**验证器 P0 = 无北向闸门基线**，北向 +0.101 单独计量。

### 7) 交 18:00 PM-EVOLVE 的下一步聚焦

1. **重启引擎使僵尸冻结修复生效**（最高优先级，用户手动）。重启后观察 13:00 后是否
   出现建仓；若仍 0 成交，则剩余阻力才是「trend_up 闸门」，届时再判。
2. **建议补做**：`_halted/_halt_reason/_halt_day` 持久化（需 `engine_state` 加列）。
   当前缺口：真熔断后重启会丢失 halt 标志 → 冷却期被绕过（既有行为，非本轮引入）。
3. **alpha ≥ −30pt 主 KPI**：参数与已试结构性方向持续证伪，本轮再证 2 项。
   建议 PM 周期评估「扩宇宙 / 跨板块降单一高β暴露」这一**结构性**方向（非调参）。
4. **数据新鲜度**：日线末端仍为 **2026-08-25**（滞后约 4 周），所有结论均在该截面下成立，
   补拉后应复跑共识复核。

> 证据：`logs/am_evolve_tvs2_2026-09-21.json`、`logs/am_evolve_dde2_2026-09-21.json`、
> `logs/am_evolve_consensus_2026-09-21.json`。

---

### 十·补（2026-09-21 15:30）首版僵尸修复的**实际缺陷**已纠正

**背景**：上午落盘的首版修复（§十 §3）**在真实重启场景下不会生效**。用户反馈「仍然没有开仓」后复核查出：

- 首版用 `_halt_day` 算冷却天数，但 `_halt_day` **不持久化**，且
  `RiskManager.__init__` 第 46 行恒把它设为 `date.today()` → 恢复后的
  `held = (today - 今天).days = 0 < halt_recover_days(1)` → **自愈永远触发不了**。
  这是「看起来修好了、重启后依旧冻结」的假修复。

**纠正（本轮实际改动）**：
1. `risk/manager.py` 新增 `_consec_loss_date`（**连亏起算日**），在 `on_fill` 首次计亏时写入、
   连亏清零时清空；`_maybe_recover_zombie()` 改用该字段算冷却。
2. **无时间戳（老库升级）时按「已过冷却」立即解封**——无法证明这段连亏是当天发生的，
   而它确实跨多个交易日滞留在 `engine_state`，继续冻结毫无风控意义。
3. 新增 `export_state()` / `load_state()`；`storage/db.py` 加 `engine_state.risk_state`
   列（幂等 `ALTER TABLE`，已在 DB 副本验证）；`engine/event_engine.py` 保存/恢复该列，
   顺带把 `_halted/_halt_reason/_halt_day` 一并持久化（上午 §7 的待办项一并完成）。
4. 顺序已验证：`on_asset_update`（event_engine.py:1267）早于 `_run_single_step`（1352），
   → **重启后首个 tick 即解封，同一 tick 内即可开仓**，不会浪费一天。

**回归**：`tests/test_risk_zombie.py` 扩到 **8 例全通过**（新增：无时间戳立即解封、
持久化往返）；`tests/run_all.py` **212 通过 / 1 既有失败 / 共 213**。
**仍需用户手动重启引擎**（当前进程 PID 19764 启于 00:16，加载的是修复前代码）。

---

### 十一、2026-09-21 16:10 OWNER 指令：paper 账本复位，2026-09-22 作为新周期第一天

**指令**：「将项目金额初始化 1000000，明天作为第一天从头计量」。

**执行**（脚本 `scripts/reset_paper_account.py`，**可逆**）：
1. 整库备份 `storage/qmt_backup_20260921_161552.db`（23.5 MB）；
2. 计量表**归档不删除** → `equity_snapshots_pre_reset`(3294) / `orders_pre_reset`(287) /
   `fills_pre_reset`(283)，主表清空；
3. `engine_state` 复位：现金 **1,000,000**、零持仓、连亏=0、日内盈亏=0、
   `peak_asset=peak_equity=1,000,000`（回撤从 0 起算）、`trade_date=2026-09-22`；
4. 写入起始权益快照 `2026-09-22 09:30 → 1,000,000`（新周期计量基线）。

**为什么额外加「复位标志」机制**：引擎每 60s 把内存状态写回 `engine_state`，
直接在库里改会被覆盖。故新增 `storage/.reset_paper.flag`：引擎启动时在**内存中**
再复位一次（`EventEngine._apply_pending_reset` → `RiskManager.reset_all`）并自动删除标志，
同时清掉复位前的脏快照（避免曲线出现「80 万→100 万」的假暴涨）。**幂等，对重启时序不敏感**。

**复位前状态（留档）**：803,680.18 / −19.63% / 0 持仓 / 0 成交（09-16 强平后冻结）。

**回归**：212 通过 / 1 既有失败 / 共 213。`EXECUTION_MODE` 保持 paper，风险底线未动。
**仍需用户重启引擎**（本次改动全部在引擎启动/初始化路径上）。
