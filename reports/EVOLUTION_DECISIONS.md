# EVOLUTION_DECISIONS — 自进化迭代决策账本

> 由【自进化执行器】自动化（d8013e93，交易日 18:00）**仅追加**写入。
> 记录每轮「候选改动 → IS/OOS 证据 → 闸门裁决 → 落盘/否决」的完整链路，历史一律不改写。
> 机读证据：`reports/_evolve_r3_consensus.json`；验证器：`strategy/_evolve_wf.py`。

---

## 2026-09-17（第 1 轮，执行器首次运行）

### 一、当前 vs 目标差距

| 考核指标 | 目标 | 当前 | 差距 |
|---|---|---|---|
| walk-forward 样本外均值 Sharpe | ≥ 2.0 | **+1.015**（P0 基线，90x6 折） | **-0.985** |
| walk-forward 正收益折数 | ≥ 6/7 | 5/6（≈ 5.8/7 折算） | 略低 |
| walk-forward 最大回撤 | ≥ -22% | -19.74%（均值）/ IS -34.29% | 均值达标，IS 超标 |
| live paper 近 4 周滚动收益 | > 0% | **-19.63%**（9-02 100 万 → 9-16 80.37 万） | **-19.63pt** |

> 注：账本 `logs/evolution_ledger.jsonl` 目前仅 1 条（2026-09-16），不足 4 周窗口，
> 该目标暂以区间收益 -19.63% 近似判定，明确未达标。
> 另注：本轮回测口径为 25 只 AI 宇宙 × 713 根日线（2022-12-08→2026-08-25，
> 数据末端到今日约 3 周滞后），与历史上「+339.9% / Sharpe 1.69」的口径（500 根短窗）
> 不同，故绝对数值不可直接横比；**所有结论一律以同口径基线 P0 的相对增量为据**。

### 二、本轮候选（先怀疑收益真正的杀手）

针对 backlog 与账本暴露的杀手——「退出逻辑在下跌市太慢 / 隔夜跳空暴露过多 /
选股集中度过高等」——先做 6 维 OOS 网格扫描，再做候选对比：

| 候选 | 来源假设 | OOS 均值 Sharpe 增量 | 结论 |
|---|---|---|---|
| trend_trailing（宽幅移动止损） | 退出太慢、浮盈回吐 | **-0.56 / -0.78** | 否决（0/6 折胜，-223pt） |
| dd_ctrl（回撤期动态减仓） | 尾部暴露 | -0.11 | 否决 |
| reentry_cooldown=5（再入场冷静期） | 换手/whipsaw 成本侵蚀 | +0.068 | 正向但不足 |
| hard_stop_pct=-0.12 | 隔夜缺口暴露 | +0.02 | 中性 |
| trend_exit_ma / momentum_lookback | 参数微调 | 最优即当前值 | 否决（无增量） |
| **momentum_top_n=3** | **前 6 名含「动量尾巴」，拖累风险调整后收益** | **+0.216（均值）** | **通过** |

### 三、IS / OOS 数据对比（momentum_top_n 6 → 3）

**样本内 IS（全样本 713 根，含单边 0.15% 成本）**

| 指标 | P0（top_n=6） | C1（top_n=3） | 变化 |
|---|---|---|---|
| 总收益 | +265.63% | **+268.59%** | +2.96pt |
| Sharpe | +1.27 | **+1.35** | +0.08 |
| 最大回撤 | -34.29% | **-24.00%** | **收窄 10.29pt** |
| alpha | +125.70pt | +128.66pt | +2.96pt |
| 成交数 | 107 | 85 | -22（-20.6%） |
| 胜率 | 37.4% | 38.8% | +1.4pt |
| 暴露 | 91.8% | 90.2% | -1.6pt |

**样本外 OOS（4 套互不重叠折划分，取最差窗口防运气）**

| 折划分 | P0 均值 Sharpe | top_n=3 均值 Sharpe | 增量 | 累计收益差 |
|---|---|---|---|---|
| 60 × 9 折 | +0.818 | +1.161 | **+0.343** | +175.6pt |
| 75 × 7 折 | — | — | **+0.094** | — |
| 90 × 6 折 | +1.015 | +1.298 | **+0.283** | +77.1pt |
| 120 × 4 折 | +0.920 | +1.064 | **+0.144** | +18.0pt |
| **均值** | — | — | **+0.216** | — |

- OOS 均值最大回撤：-19.74% → **-13.86%（最差窗口）**
- OOS 最差单折：-7.9% → **-8.2%**（仍远优于 -15% 灾难线）
- 正收益折数：5/6 → 5/6（90x6）；60x9 口径下 7/9
- IS/OOS Sharpe 差距：|1.173 - 1.35| / 1.35 = **13.1%**（≤ 25%，无过拟合迹象）

### 四、闸门裁决

| 闸门 | 判据 | 实测 | 结果 |
|---|---|---|---|
| ① | IS 收益或 Sharpe 提升 | ret +2.96pt、Sharpe +0.08 | **PASS** |
| ② | OOS 均值 Sharpe 提升 ≥ +0.10 | **+0.216** | **PASS** |
| ③ | OOS/IS Sharpe 差距 ≤ 25% | 13.1% | **PASS** |
| ④ | walk-forward 最大回撤 ≥ -22% | -13.86% | **PASS** |
| ⑤ | 任一单折收益 > -15% | -8.2% | **PASS** |
| ⑥ | `tests/run_all.py` 全通过 | 197 / 0（落盘后复跑） | **PASS** |
| ⑦ | 风险底线不动 | max_drawdown_pct=-0.20（≤-0.15）、risk_per_trade=0.012（≤0.02）、T1_RESTRICTION=True、max_positions=5（≥3）——**全未改动** | **PASS** |
| ⑧ | EXECUTION_MODE 保持 paper | paper | **PASS** |

**结论：8/8 全通过 → 落盘。**

### 五、实际落盘的改动

| 文件 | 参数 | 原值 | 新值 | 实盘生效 |
|---|---|---|---|---|
| `config/settings.py` | `STRATEGY_PARAMS.momentum_top_n` | `6` | **`3`** | 是（`engine/event_engine.py:1376` 直接读取） |

单点改动、零代码改动、可逆（改回 6 即恢复旧行为）。已在 settings.py 内联写入证据与机理注释。

### 六、本轮否决 / 暂存项

- **trend_trailing（趋势模式移动止损）**：**强证伪**。3 套参数下 0/6 折胜、累计 -223 ~ -248pt，
  6 折 MDD 反而恶化。机理：本宇宙主升段长，宽幅移动止损在强势回调中被反复震出，
  与「趋势骑行」范式直接冲突。**加入已证伪清单，勿重复提交。**
- **reentry_cooldown=5（再入场冷静期）**：证据正向（OOS 均值 Sharpe +0.068；
  与 top_n=3 组合后最差窗口 +0.157 vs 单独 +0.094，胜率 37.4%→42.3%、成交 -27%）。
  **但 `reentry_cooldown` 目前仅存在于回测器 `BacktestConfig`，实盘引擎未实现**——
  若写入 settings.py 会造成「回测有效、实盘无效」的口径背离。
  **暂存为下一轮候选：需先在 `engine/event_engine.py` 实现「离场后 N 个交易日内不重开仓」，
  再走同一套闸门落盘。**
- **max_positions=3**：IS 收益 -83pt，否决；`max_positions` 维持 5（top_n=3 已使实际持仓 ≤3，
  再压 max_positions 会在 F4 强趋势折损失 -105.5pt）。
- **momentum_top_n=2**：IS Sharpe 最高（1.37）但收益 -21pt 且单折波动更大（-20.3pt），
  属过度集中，不采纳。

### 七、下一步聚焦方向

1. **补齐 live paper 4 周样本**：账本目前仅 1 条，无法判定 4 周滚动收益，后续以逐日累积为准。
2. **实现 `reentry_cooldown=5`（最高优先级）**：唯一尚未落地且证据正向的结构性改动，
   需先补实盘引擎实现。预计组合后 OOS 均值 Sharpe 增量可达 **+0.245**。
3. **复核 2026-09-16 去风险调参的代价（重要发现）**：`risk_per_trade` 由 0.02 收紧到 0.012 后，
   OOS 均值 Sharpe 仅 +0.027（近乎不变），但 **OOS 累计收益 -103.4pt、均值收益 +51.1%→+33.9%**。
   即该次收紧**几乎没换来风险调整后收益，却付出约 100pt 收益**。
   闸门⑦ 允许 risk_per_trade ≤ 0.02，回补至 0.016~0.020 是下一轮高价值候选
   （0.016 时 OOS Sharpe 已最优 +1.053，且 MDD -17.63% 优于 -22% 底线）。
4. **数据新鲜度**：xtdata 日线仅到 2026-08-25（滞后约 3 周），建议在可达 xtdata 的桌面持久会话中
   补拉后重跑本验证器，确认结论在最新窗口仍成立。

---

## 2026-09-18（第 2 轮 · **上午周期 AM-EVOLVE** · 午休窗口）

> 执行器：789d671e（11:40 午休）。与 18:00 下午周期的分工：本轮优先**热读参数**
> （改动 13:00 开盘即生效），并产出半日度量供 15:35 OBSERVE / 18:00 PM-EVOLVE 消费。

### 〇、环境校验

| 项 | 值 | 结论 |
|---|---|---|
| `EXECUTION_MODE` | `paper` | ✅ 未动 |
| `max_drawdown_pct` | -0.20（≤ -0.15） | ✅ |
| `risk_per_trade` | 0.012（≤ 0.02） | ✅ |
| `T1_RESTRICTION` | True | ✅ |
| `max_positions` | 5（≥ 3） | ✅ |

### 一、当前 vs 目标差距（本轮**重算**，基线已修正）

**⚠️ 口径修正（本轮最重要的前置动作）**：`strategy/_evolve_wf.py::base_cfg()` 此前仍停留在
`momentum_top_n=6 / risk_per_trade=0.02`，与已落盘的生产配置（**top_n=3 / rpt=0.012**）不一致。
若不修正，上一轮 top_n=3 的收益会被**重复计入**本轮增量。现已对齐为真实生产配置，
**本轮所有 dSh 一律相对修正后的 P0 计算**。

修正后 P0 基线（25 只 AI 宇宙 × 713 根日线，2022-12-08→2026-08-25，单边 0.15% 成本）：
IS ret **+158.97%** / Sharpe **+1.25** / MDD **-16.08%**。

| 考核指标 | 目标 | 当前（4 套折最差窗口口径） | 差距 |
|---|---|---|---|
| wf 样本外均值 Sharpe | ≥ 2.0 | **+1.105**（120×4 最差；4 窗均值 +1.173） | **-0.83** |
| wf 正收益折数 | ≥ 6/7 | 7/9、5/7、6/6、3/4（折算 5.0~6.0/7） | 仅 90×6 达标 |
| wf 最大回撤 | ≥ -22% | **-10.48%**（最差窗口） | **已达标 ✅** |
| live paper 近 4 周 | > 0% | **-19.63%**（账户冻结，见二） | **-19.63pt** |

### 二、半日度量（2026-09-18 上午，实测）

- 期末权益 **803,680.18**（= `day_open_asset`，上午盈亏 **0.00%**）；持仓 **0** 只；成交 **0** 笔。
- 对比昨日（09-17）收盘 803,680.18：跨日 **0.00%**；区间累计（09-02 起 100 万）**-19.63%**。
- 数据源：`storage/qmt.db` 的 `equity_snapshots`（今日 135 条）+ `engine_state`（id=1）。
- **关键发现（根因已定位，非故障）**：静态池 12 只全量复现评分，**全部 `trend_up=False`**，
  日线闸门整体关闭（典型 `reason=daily-gate:bias=-1.00 trend_up=False`）；
  动量前 3 名 002371/002415/000977 得分 3.84/3.78/3.88，均卡在 `score<4.0`。
  → **零开仓是「全宇宙跌破趋势 MA」下的策略预期防御行为，与回测同口径，不是运行缺陷。**
  含义：在宇宙恢复上升趋势前，paper 账户维持 100% 现金、收益冻结，
  「近 4 周 >0%」目标**无法通过参数改动**推进。

### 三、本轮候选与 IS/OOS 证据（10 维 OOS 网格，90×6 折）

| # | 参数（当前值） | 扫描值 | OOS 最优 | dSh | 累计差 | 实盘读取点 | 闸门② |
|---|---|---|---|---|---|---|---|
| 1 | `hard_stop_pct`(-0.18) | -0.10…-0.28 | **-0.18（即当前）** | +0.000 | 0.0pt | 热读 4 处 | ✗ |
| 2 | `momentum_lookback`(60) | 20…120 | **60（即当前）** | +0.000 | 0.0pt | 热读 | ✗ |
| 3 | `momentum_top_n`(3) | 2,3,4,5,6 | **3（即当前）** | +0.000 | 0.0pt | 热读 | ✗ |
| 4 | `trend_exit_ma`(60) | 20…100 | **60（即当前）** | +0.000 | 0.0pt | 引擎未读取 | ✗ |
| 5 | `max_positions`(5) | 3,4,5,6,8 | **5（即当前）** | +0.000 | 0.0pt | 热读 | ✗ |
| 6 | `trend_max_hold_days`(120) | 40…999 | **120（即当前）** | +0.000 | 0.0pt | 引擎未读取 | ✗ |
| 7 | `risk_per_trade`(0.012) | 0.010…0.020 | 0.016 | **+0.027** | +87.8pt | **缓存，需重启** | ✗ |
| 8 | `reentry_cooldown`(0) | 0,3,5,8,12,20 | 3 | **+0.021** | -3.9pt | **回测独有** | ✗ |
| 9 | `buy_score_threshold`(4.0) | 3.0…5.0 | 4.5 | **+0.021** | +2.9pt | 回测独有 | ✗ |
| 10 | `min_signals`(3) | 2,3,4,5 | 4 | **+0.006** | +2.2pt | 回测独有 | ✗ |

补充：`fixed_amount`（回测 30 万 vs 生产 `max_position_amount` 18 万）
30 万→18 万 对应 dSh **-0.002**、OOS 累计 **-12.5pt** —— 09-16 去风险的仓位上限收紧
代价约 12.5pt 收益、Sharpe 几乎不变，但同样远达不到 +0.10 闸门。

### 四、闸门裁决：**全部 REJECTED（0 / 10 通过）**

无一候选满足闸门②（OOS 均值 Sharpe 提升 ≥ +0.10）。最优者 `risk_per_trade=0.016`
仅 **+0.027**，且 IS MDD 由 -16.08% 恶化到 -20.77%。
**结论：本轮不改动任何策略参数，`config/settings.py` 与 `engine/` 保持零改动。**

### 五、两条重要负面知识（务必保留，勿重复踩坑）

1. **`reentry_cooldown` 的上一轮正向证据已失效**。上一轮「OOS +0.068 / 与 top_n=3 组合 +0.245」
   是相对**旧基线**（top_n=6、rpt=0.02）测得的；在 top_n=3 落盘后的修正基线下，
   最优（cd=3）仅 **+0.021**、累计差 **-3.9pt**，cd≥8 全线转负。
   → **撤销其「最高优先级下一步」地位，不必再补实盘引擎实现。**（上一轮 §七.2 作废）
2. **`risk_per_trade` 近似「杠杆旋钮」而非 alpha 旋钮**：0.010→0.020 区间内
   OOS 均值 Sharpe 始终在 +1.30~1.35 窄幅波动，而累计收益单调 +140pt、MDD 同步恶化。
   → 想提 Sharpe 只能改**信号/进出场结构**，调仓位规模只会等比放大收益与风险。
   09-16 收紧到 0.012 在 Sharpe 上近乎无损，**不建议回补**。

### 六、本轮实际改动（仅验证工具，非策略）

| 文件 | 改动 | 性质 |
|---|---|---|
| `strategy/_evolve_wf.py` | `base_cfg()` 对齐生产（top_n 6→3、rpt 0.02→0.012）+ 负值 `--values` 用法说明 | SAFE（工程/口径），零策略影响 |

`tests/run_all.py`：**197 / 0 全通过**（改动后复跑）。

### 七、下一步聚焦方向（交 18:00 下午周期）

1. **参数空间已确认耗尽**：10 维全处高原最优，wf Sharpe 天花板约 **+1.33**（90×6）/
   最差窗口 **+1.105**，距目标 2.0 差 **-0.83**。**建议 OWNER 裁决**：是修订目标，
   还是投入结构性 alpha（新信号源/新退出结构）——继续调参已被本轮系统性证伪。
2. **paper 账户被「全宇宙 trend_up=False」冻结**（-19.63%，100% 现金）。
   这是当前唯一能移动「近 4 周 >0%」的现实路径，但取决于市场而非参数；
   建议 PM 周期把「闸门关闭时长/踏空成本」做成度量项，而非放宽闸门（放宽属已证伪方向）。
3. 数据新鲜度：日线末端 2026-08-25（滞后约 3 周），建议在可达 xtdata 的桌面会话补拉后复跑本验证器。
